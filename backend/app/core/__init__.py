"""Core domain models shared across Stage 1, Stage 2, and the API layer.

Defense-only contract: every recommendation enum is restricted to
observe/explain/review actions. There is deliberately NO "block" state.
"""
